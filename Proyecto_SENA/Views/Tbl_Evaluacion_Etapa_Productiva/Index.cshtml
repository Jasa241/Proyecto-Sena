@model IEnumerable<Proyecto_SENA.Models.Tbl_Evaluacion_Etapa_Productiva>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Juicio_Evaluacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reconocimientos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Aprendices.Numero_Identificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Juicio_Evaluacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reconocimientos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Aprendices.Numero_Identificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Evaluacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Evaluacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Evaluacion })
        </td>
    </tr>
}

</table>
